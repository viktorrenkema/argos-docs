---
title: The tooltip is built from t...
---

The tooltip is built from three pieces on `BlockDiffData`: `blockSemanticName` (header), `changeDescription` (text body), `changeItems` (rich body). `changeItems` takes priority over `changeDescription` when both exist.eee

